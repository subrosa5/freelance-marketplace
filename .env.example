# Database (Neon serverless PostgreSQL)
DATABASE_URL="postgresql://user:pass@host/db?schema=public&sslmode=require"
DIRECT_URL="postgresql://user:pass@host/db?schema=public&sslmode=require"

# Auth
JWT_SECRET="your-secret-here-min-32-chars"

# Pusher (pusher.com — free tier)
PUSHER_APP_ID=""
PUSHER_SECRET=""
NEXT_PUBLIC_PUSHER_KEY=""
NEXT_PUBLIC_PUSHER_CLUSTER="eu"

# Stripe (stripe.com — test mode is free)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# Uploadthing (uploadthing.com — free tier 2GB)
UPLOADTHING_TOKEN=""

# Upstash Redis (upstash.com — free tier)
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""

# Resend (resend.com — free 3000 emails/month)
RESEND_API_KEY="re_..."

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
